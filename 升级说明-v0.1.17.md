# 蓍草 v0.1.17 · 基础概念封版补丁

## 基准版本

本补丁用于：

- 蓍草 v0.1.16

请直接将补丁内容覆盖到项目根目录。

## 本次内容

本版按《蓍草 · 基础概念阶段总整理 v0.2》将已定稿的共同基础概念正式接入现有知识库。

新增：

- 五行：旺相休囚死；
- 天干：天干五合；
- 地支：地支概述、子至亥、藏干、六合、六冲、相刑、相害、三合、三会（方合）；
- 干支纪序；
- 十二长生。

同步调整：

- 天干概述删除“合而不敢合也，有若无也”的应用层说明；
- 天干概述明确“相合与实际形成化气不是同一层次”；
- 地支概述按总整理稿压缩藏干与六类关系的重复维护；
- 十二地支正文统一使用“六十甲子”为主名，经典原文中的“六十花甲子”保持原文；
- 《天干条目专项编辑规范》更新至 v0.2；
- 《地支条目专项编辑规范》更新至 v0.2；
- 《规范体系索引》更新至 v0.1.2；
- README 与版本号更新至 v0.1.17。

## 分类边界

- 旺相休囚死归入“五行”；
- 禄、羊刃不作为八字与六爻共同基础概念顶层条目；
- 禄、羊刃后续进入命理知识层；
- 现有单干、单支中的禄刃配属暂作为下游关联保留。

## 本版未做

本版不同时进行 Registry/数据层的大规模重构。六十甲子、藏干、地支关系、十二长生等结构化事实后续再迁移为单一结构化事实来源。

## 校验

发布前执行：

```bash
npm run audit
npm test
npm run build
```

## 兼容性修正

v0.1.16 中 `scripts/static-audit.mjs` 会扫描条目目录内未被 `entry.ts` 挂载的旧 Markdown 文件，从而把孤立文件中的脚注标记误报为当前知识条目的校验错误。本补丁将静态审计范围收束为 `entry.ts` 实际引用的 Markdown section；运行时 Registry 本身原本即只加载已挂载 section，因此该修正使静态审计与运行时加载边界一致。

## 本次实际校验结果

已完成：

- `npm run audit`：通过；
- 静态审计结果：42 entries / 227 referenced Markdown sections / 32 sources；
- `src/knowledge` 与新增 API 测试共 102 个 TypeScript 文件独立 syntax/transpile 检查：通过；
- 新增 Knowledge ID、分类 overview、正文 Knowledge 引用、脚注来源均通过静态审计。

当前执行环境无法完成 Vitest / Vite 全量运行：v0.1.16 压缩包自带的 `node_modules` 缺少当前 Linux 环境所需的 Rollup optional native dependency，且当前环境无法重新安装完整依赖。因此本次不标记“完整测试/构建通过”。

本地覆盖补丁后请在项目根目录重新安装依赖并执行：

```bash
npm install
npm run audit
npm test
npm run build
```
